# 🌦️ Python Weather App

A lightweight desktop application that fetches real-time weather data for any city using the OpenWeatherMap API. Built with Python and Tkinter.

## 🖼️ Features

- **Real-Time Data:** Fetches live temperature and weather conditions.
- **GUI Interface:** Simple and easy-to-use window design.
- **Smart Error Handling:** Warns the user if the city is not found or if there is no internet connection.

## 🛠️ Requirements

You need Python installed and the `requests` library.

To install the required library, run:

    pip install requests

## 🎮 How to Use

1.  **Run the App:**

    ```bash
    python main.py
    ```

2.  **Enter Location:**
    Type a city name (e.g., "London", "Ankara", "Tokyo") into the box.

3.  **Get Results:**
    Click the **"Enter"** button to see the current temperature and weather status.

## 📂 Project Structure

    ├── main.py          # Source code
    ├── requirements.txt # Dependencies
    ├── README.md        # Documentation
    └── .gitignore       # System files to ignore

---

_Powered by OpenWeatherMap API_ 🌍
